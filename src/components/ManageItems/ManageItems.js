import React from 'react';
import Loading from '../Loading/Loading';

const ManageItems = () => {
    return (
        <div>
            <h1>Manage Items</h1>
            <Loading />
        </div>
    );
};

export default ManageItems;